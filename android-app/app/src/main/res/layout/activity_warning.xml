<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#7F1D1D"
    android:padding="32dp">

    <!-- Alert Emoji -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="⚠️"
        android:textSize="80sp"
        android:layout_marginBottom="24dp"/>

    <!-- Warning Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Security Protection Disabled!"
        android:textColor="#FFFFFF"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp"/>

    <!-- Warning Description -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Device Administrator permissions have been deactivated. Remote screen lock, alarm override, and anti-theft services are currently disabled.\n\nYou must re-enable Device Administrator permissions immediately to unlock your device."
        android:textColor="#FECDD3"
        android:textSize="15sp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.3"
        android:layout_marginBottom="36dp"/>

    <!-- Activation Button -->
    <Button
        android:id="@+id/btn_re_enable_admin"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:text="Enable Device Admin 🔒"
        android:backgroundTint="#FFFFFF"
        android:textColor="#7F1D1D"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:elevation="4dp"/>

</LinearLayout>
